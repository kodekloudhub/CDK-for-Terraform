// Exporting a named function
export function add(a: number, b: number): number {
  return a + b;
}

// Exporting another named function
export function subtract(a: number, b: number): number {
  return a - b;
}
